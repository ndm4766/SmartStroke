<Page
    x:Class="SmartStroke.TrailsTest"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SmartStroke"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>

        <!--TextBlock Name="timer_box" Foreground="Black" Text="" FontSize="50" VerticalAlignment="Center" HorizontalAlignment="Right" Grid.Row="0" /-->
        <Grid Name="canvasGrid" Grid.Column="0" >
                    <Canvas Name="MyCanvas" Background="Transparent" Margin="10"/>
        </Grid>

        <Grid Name="optionsGrid" Grid.Column="1" >
            <Viewbox>
                <Grid>
                    <Button Name="saveButton" Content="Save" Margin="35,284.375,0,433.625" Width="160" Height="80" Visibility="Visible" Click="btnScreenshot_Click" IsHitTestVisible="False" RenderTransformOrigin="0.5,0.5" UseLayoutRounding="False" d:LayoutRounding="Auto"  FontFamily="Segoe UI Light" Background="#203864" Foreground="#FFFFFF" >
                        <Button.RenderTransform>
                            <CompositeTransform Rotation="90"/>
                        </Button.RenderTransform>
                    </Button>
                    <Button Name="submitButton" Content="Submit" Margin="35,85.119,0,601.538" Width="160" Height="80" Visibility="Visible" Click="SubmitButtonClicked" IsHitTestVisible="False" RenderTransformOrigin="0.5,0.5" UseLayoutRounding="False" d:LayoutRounding="Auto" FontFamily="Segoe UI Light" Background="#203864" Foreground="#FFFFFF" >
                        <Button.RenderTransform>
                            <CompositeTransform Rotation="90"/>
                        </Button.RenderTransform>
                    </Button>
                    <Button x:Name="cancelButton" Content="Cancel Test" Margin="35,467.506,0,250.494" Width="160" Height="80" Visibility="Visible" Click="cancelTest" RenderTransformOrigin="0.5,0.5" UseLayoutRounding="False" d:LayoutRounding="Auto" FontFamily="Segoe UI Light" Background="#203864" Foreground="#FFFFFF" >
                        <Button.RenderTransform>
                            <CompositeTransform Rotation="90"/>
                        </Button.RenderTransform>
                    </Button>
                </Grid>
            </Viewbox>
        </Grid>

    </Grid>
</Page>
