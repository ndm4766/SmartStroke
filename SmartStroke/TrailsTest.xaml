<Page
    x:Class="SmartStroke.TrailsTest"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SmartStroke"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="55*"/>
            <RowDefinition Height="329*"/>
        </Grid.RowDefinitions>

        <TextBlock Name="timer_box" Foreground="Black" Text="0:00:00" FontSize="50" VerticalAlignment="Center" HorizontalAlignment="Right" Grid.Row="0" />
        <Viewbox HorizontalAlignment="Left" VerticalAlignment="Top" Grid.RowSpan="2">
            <Canvas Name="MyCanvas" Background="Transparent" />
        </Viewbox>
        <Button Name="submitButton" Content="Submit" Margin="1004,255,0,353" Width="100" Height="50" Visibility="Collapsed" Click="SubmitButtonClicked" Grid.Row="1"/>

    </Grid>
</Page>
