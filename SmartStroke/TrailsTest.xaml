<Page
    x:Class="SmartStroke.TrailsTest"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SmartStroke"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>

        <!--TextBlock Name="timer_box" Foreground="Black" Text="" FontSize="50" VerticalAlignment="Center" HorizontalAlignment="Right" Grid.Row="0" /-->
        <Grid Name="canvasGrid" Grid.Column="0" >
            <Viewbox>
                <Grid>
                    <Canvas Name="MyCanvas" Background="Transparent" Margin="10"/>
                </Grid>
            </Viewbox>
        </Grid>
        
        <Grid Name="optionsGrid" Grid.Column="1" >
            <Viewbox>
                <Grid>
                    <Button Name="saveButton" Content="Save" Margin="78.135,191.835,0,526.165" Width="100" Height="50" Visibility="Visible" Click="btnScreenshot_Click" IsHitTestVisible="False" RenderTransformOrigin="0.5,0.5" UseLayoutRounding="False" d:LayoutRounding="Auto"  FontFamily="Segoe UI Light" Background="#203864" Foreground="#FFFFFF" >
                        <Button.RenderTransform>
                            <CompositeTransform Rotation="90"/>
                        </Button.RenderTransform>
                    </Button>
                    <Button Name="submitButton" Content="Submit" Margin="78.135,69.447,0,648.553" Width="100" Height="50" Visibility="Visible" Click="SubmitButtonClicked" IsHitTestVisible="False" RenderTransformOrigin="0.5,0.5" UseLayoutRounding="False" d:LayoutRounding="Auto" FontFamily="Segoe UI Light" Background="#203864" Foreground="#FFFFFF" >
                        <Button.RenderTransform>
                            <CompositeTransform Rotation="90"/>
                        </Button.RenderTransform>
                    </Button>
                    <Button x:Name="cancel" Content="Cancel Test" Margin="72.164,329.148,0,388.852" Width="116.418" Height="50" Visibility="Visible" Click="cancelTest" RenderTransformOrigin="0.5,0.5" UseLayoutRounding="False" d:LayoutRounding="Auto" FontFamily="Segoe UI Light" Background="#203864" Foreground="#FFFFFF" >
                        <Button.RenderTransform>
                            <CompositeTransform Rotation="90"/>
                        </Button.RenderTransform>
                    </Button>
                </Grid>
            </Viewbox>
        </Grid>
        
    </Grid>
</Page>
